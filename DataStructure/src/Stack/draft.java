package Stack;

import static Stack.Exp_evaluation_version1.rating_operator;
import java.util.Scanner;

public class draft {

    public static void main(String[] args) {
        /*
        
         if (stack.isEmpty()) {
         stack.push(array[i]);
         } else {

         while (!stack.isEmpty()) {
         int operator_degree = 0;
         int last_operator_degree = 0;

         operator_degree = rating_operator(array[i]);
         last_operator_degree = rating_operator(stack.array[stack.top]);

         if (operator_degree > last_operator_degree) {

         stack.push(array[i]);
         break;

         } else {

         stack2.push(array[i]);
         newArray[i] = stack.pop();

         // المشكلة هنا بقي اني عايز افضل اكرر الحوار ده لحد ما الاستاك يفضي
         }
         }

         }

         }
         */

    }

}
